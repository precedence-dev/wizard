{
  "name": "@precedence-dev/wizard",
  "version": "0.2.0",
  "description": "Interactive setup: scan a repo, pick outcomes, instrument them. The one-command path into Precedence.",
  "license": "FSL-1.1-ALv2",
  "author": "Precedence",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/precedence-dev/wizard.git"
  },
  "homepage": "https://github.com/precedence-dev/wizard",
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "precedence-wizard": "bin/precedence-wizard.js"
  },
  "files": [
    "dist/",
    "bin/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && tsc",
    "prepare": "npm run build",
    "test": "npm run build && node test/invariants.mjs"
  },
  "dependencies": {
    "@precedence-dev/cli": "^0.2.0",
    "@precedence-dev/instrument": "^0.2.0",
    "@precedence-dev/viewer": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3"
  }
}
